include $(top_srcdir)/build/Makefile.am.common

plugindir = $(pkglibdir)/httpserver
plugin_PYTHON = \
	httpserver.py

plugin_DATA = httpserver.plugin
EXTRA_DIST += $(plugin_DATA)

-include $(top_srcdir)/git.mk

